import styles from './Footer.module.css';

function Footer() {
  return (
    <div className={styles.footer}>
      <span className={styles.team}>Team.SKT</span>
    </div>
  );
}

export default Footer;
